version: '3.1'

services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: h723aHdsa73
    networks: 
      - dbnet
  node:
    image: nodeapp:latest
    volumes:
      - .:/var/node-app
      - /var/node-app/node_modules
    networks:
      - dbnet
    ports:
     - 8080:8080
    command: node ./src/server/app.js
    depends_on:
      - db
networks:
  dbnet: